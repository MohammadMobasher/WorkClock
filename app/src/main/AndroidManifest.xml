<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohammad.workclock2">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity android:name=".main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.j_new_project"></activity>
        <activity android:name=".Activities.j_edit_project"></activity>
        <activity android:name=".Activities.test"></activity>
        <activity android:name=".Activities.test2"></activity>
        <activity android:name=".Activities.j_new_clock_main"></activity>
        <activity android:name=".Activities.j_new_clock_timer"></activity>
        <activity android:name=".Activities.j_edit_clock"></activity>

    </application>

</manifest>